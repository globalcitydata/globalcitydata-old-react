import React from 'react';
import {Button} from '@material-ui/core';

export default function Contact() {
    return (
        <Button variant="contained" color="primary">I am the contact</Button>
    )
}